
//$('.modal-body').html('<%#= escape_javascript(render "show", :object => @sandwich, :formats => [:html]) %>');
//$('.modal-body').html('<%#= escape_javascript(render :partial =>'form', :locals => { :order => @order, :sandwich => @sandwich }, :formats => [:html]) %>');

$('.modal-body').html('<%= escape_javascript(render :partial =>'form', :formats => [:html]) %>');

$('#modalSandwichLabel').text('<%= h @sandwich.name %>');
//var $modal = $('#modal-window'); // your selector; cache it; only query the DOM once!
$('#modal-window').modal('show'); // show modal; this happens after 3 seconds 